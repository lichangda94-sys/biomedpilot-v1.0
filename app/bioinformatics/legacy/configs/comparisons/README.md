# Comparison Configs

- Store dataset-specific comparison configs here as `configs/comparisons/{dataset_id}.json`.
- Keep fields aligned with `configs/standards/asset_contract_v1.yaml`.
- This directory is intentionally empty in the first Module 4 pass; loaders may return `null` when no config exists.
