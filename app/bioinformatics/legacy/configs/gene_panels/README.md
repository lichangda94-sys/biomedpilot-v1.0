# Gene Panel Configs

- Store reusable gene panels here as `configs/gene_panels/{panel_id}.json`.
- Keep panels data-only and avoid embedding workflow logic.
- This directory is intentionally empty in the first Module 4 pass; loaders may return `null` when no panel exists.
