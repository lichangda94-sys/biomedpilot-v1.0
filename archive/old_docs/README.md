# Old Docs Archive

This folder is reserved for older documentation selected for archival review.
Current audit kept older docs inside `archive/legacy_sources/*/docs/` and recorded phase-snapshot directories under `archive/duplicate_candidates/` instead of copying every old snapshot into active docs.
